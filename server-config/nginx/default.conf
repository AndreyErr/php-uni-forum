server {
    listen 8080;
    server_name localhost;
    location / {
        proxy_pass http://app.app:8081;
        proxy_cache off;
    } 
    location ~ ^/(about.html|src|errors) {
        root /var/www/app/;
        proxy_cache off;
    }
}